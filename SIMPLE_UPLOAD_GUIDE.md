# Simple Upload Guide - No Drag & Drop Needed!

## 🎯 Easiest Way: Create ZIP File

### Step 1: Create ZIP File

1. **Open File Explorer** (Press `Win + E`)
2. **Navigate to:** `D:\jivaka`
3. **Right-click** on the **`backend`** folder
4. **Select:** "Send to" → "Compressed (zipped) folder"
5. **Wait a moment** - it will create `backend.zip`

### Step 2: Upload ZIP to Vercel

1. **In Vercel** (still on Add New Project page)
2. **Look for:** "Upload" or "Choose Files" button
3. **Click "Browse"** or **"Choose Files"**
4. **Select:** `D:\jivaka\backend.zip` file
5. **Click "Open"**
6. **Vercel will automatically extract it!**

---

## 🎯 Alternative: Use Browse Button

### Step 1: In Vercel Project Page

1. Click **"Add New..."** → **"Project"**
2. Look for **"Browse"**, **"Choose Files"**, or **"Upload"** button
3. **Click it** - a file picker will open

### Step 2: Select Your Folder

1. In the file picker:
   - Navigate to: `D:\jivaka`
   - **Click on** the **`backend`** folder
   - Click **"Select Folder"** or **"Open"**

**Note:** Some file pickers only let you select files, not folders. In that case:
- Go inside the `backend` folder
- Select **ALL files** (Ctrl + A)
- Click **"Open"**

---

## 🎯 Option 3: Manual File Selection

If you can only select files (not folders):

1. **In Vercel**, click **"Browse"**
2. Navigate to: `D:\jivaka\backend`
3. **Inside backend folder**, select these files:
   - `server.js`
   - `package.json`
   - `.env.example` (if it exists)
   - **All files** in the folder (Ctrl + A)
4. Click **"Open"**

---

## ✅ What to Look For in Vercel

When you're on the "Add New Project" page, look for:

- **"Browse"** button
- **"Choose Files"** button
- **"Upload"** button
- **File picker icon** (folder icon)
- **"Select folder"** option

Any of these will work!

---

## 💡 Best Method: ZIP File

**I recommend creating a ZIP file** - it's the most reliable method!

1. Right-click `backend` folder → "Send to" → "Compressed folder"
2. Upload the ZIP file
3. Vercel will extract it automatically

---

## 🆘 Still Having Trouble?

**Tell me:**
- What buttons do you see on the Vercel "Add New Project" page?
- Does it show a "Browse" button?
- What options are available?

I'll guide you based on what you see! 😊



